#include <stdio.h>

int main()
{
    float bs, da, hra, gs;

    printf("enter basic salary");
    scanf("%f", &bs);

    da = 0.4 * bs;
    hra = 0.2 * bs;
    gs = bs + da + hra;

    printf("The gross salary of Ramesh is: %f\n", gs);
    return 0;
}

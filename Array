#include <stdio.h>

int main()
{
    int marks[32], i;

    for(i = 0; i < 32; i++)
    {
        printf("Enter marks of %d student\t:", i + 1);
        scanf("%d", &marks[i]);
    }

    for(i = 0; i < 32; i++)
    {
        printf("Marks of %d are %d\n", i + 1, marks[i]);
    }

    return 0;
}
